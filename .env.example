# Local dev connection (Postgres on this box, port 5438)
DATABASE_URL="postgresql://productpop:productpop_dev@127.0.0.1:5438/productpop?schema=public"

# Production (Neon) — CTO/CEO will swap this in Vercel
# DATABASE_URL="postgresql://USER:PASS@HOST/db?sslmode=require"

# remove.bg — get a free key at https://www.remove.bg/api (50 free calls/mo)
REMOVEBG_API_KEY=""

# Public app URL (Vercel fills this in prod)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
